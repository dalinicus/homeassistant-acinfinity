info:
  name: Security Function
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/dev/ml/secFuc?devId={{AI_DEVICE_ID}}"
  params:
    - name: devId
      value: "{{AI_DEVICE_ID}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
