info:
  name: Get Device Mode Setting List
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/dev/getdevModeSettingList?port=0&devId={{DEVICE_ID}}"
  headers:
    - name: minversion
      value: "3.5"
  params:
    - name: port
      value: "0"
      type: query
    - name: devId
      value: "{{DEVICE_ID}}"
      type: query
  body:
    type: form-urlencoded
    data:
      - name: userId
        value: "{{USER_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
